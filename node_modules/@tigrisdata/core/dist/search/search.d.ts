import { SearchClient } from "../proto/server/v1/search_grpc_pb";
import { TigrisClientConfig } from "../tigris";
import { DeleteIndexResponse, IndexInfo, TigrisIndexSchema, TigrisIndexType } from "./types";
import { SearchIndex } from "./search-index";
export declare class Search {
    private readonly client;
    private readonly config;
    private readonly schemaProcessor;
    constructor(client: SearchClient, config: TigrisClientConfig);
    createOrUpdateIndex<T extends TigrisIndexType>(cls: new () => TigrisIndexType): Promise<SearchIndex<T>>;
    createOrUpdateIndex<T extends TigrisIndexType>(name: string, schemaOrClass: TigrisIndexSchema<T> | (new () => TigrisIndexType)): Promise<SearchIndex<T>>;
    listIndexes(): Promise<Array<IndexInfo>>;
    getIndex<T extends TigrisIndexType>(name: string): Promise<SearchIndex<T>>;
    deleteIndex(name: string): Promise<DeleteIndexResponse>;
    get projectName(): string;
}
