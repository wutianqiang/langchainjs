import { TigrisClient } from "./proto/server/v1/api_grpc_pb";
import { CommitTransactionResponse, RollbackTransactionResponse } from "./types";
import { Metadata } from "@grpc/grpc-js";
export declare class Session {
    private readonly _id;
    private readonly _origin;
    private readonly grpcClient;
    private readonly db;
    private readonly branch;
    private readonly _additionalMetadata;
    constructor(id: string, origin: string, grpcClient: TigrisClient, db: string, branch: string, additionalMetadata: Metadata);
    get id(): string;
    get origin(): string;
    get additionalMetadata(): Metadata;
    commit(): Promise<CommitTransactionResponse>;
    rollback(): Promise<RollbackTransactionResponse>;
}
