import { type Client, type IndexInput, type IndexPayload, type SearchInput, type TuningInput } from './types';
declare class MetalSDK implements Client {
    apiKey: string;
    indexId?: string;
    clientId: string;
    constructor(apiKey: string, clientId: string, indexId?: string);
    index(payload: IndexInput): Promise<object>;
    indexMany(payload: IndexPayload[]): Promise<object>;
    search(payload?: SearchInput): Promise<object[]>;
    tune(payload: TuningInput): Promise<object>;
    getOne(id: string, indexId?: string): Promise<object>;
    deleteOne(id: string, indexId?: string): Promise<object>;
    deleteMany(ids: string[]): Promise<object>;
}
export = MetalSDK;
