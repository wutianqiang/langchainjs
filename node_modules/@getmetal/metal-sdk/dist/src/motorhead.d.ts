import { type MotorheadClient, type MotorheadConfig, type Memory } from './types';
declare class Motorhead implements MotorheadClient {
    apiKey?: string;
    clientId?: string;
    baseUrl: string;
    constructor({ apiKey, clientId, baseUrl }: MotorheadConfig);
    addMemory(sessionId: string, payload: Memory): Promise<Memory>;
    getMemory(sessionId: string): Promise<Memory>;
    deleteMemory(sessionId: string): Promise<void>;
}
export = Motorhead;
