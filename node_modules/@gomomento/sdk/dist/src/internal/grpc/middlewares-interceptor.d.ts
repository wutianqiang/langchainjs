import { Interceptor } from '@grpc/grpc-js';
import { Middleware } from '../../config/middleware/middleware';
import { MomentoLoggerFactory } from '../../';
export declare function middlewaresInterceptor(loggerFactory: MomentoLoggerFactory, middlewares: Middleware[]): Interceptor;
