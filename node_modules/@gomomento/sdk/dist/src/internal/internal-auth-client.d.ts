import { auth } from '@gomomento/generated-types';
import grpcAuth = auth.auth;
import { ExpiresIn, RefreshAuthToken, GenerateAuthToken, TokenScope } from '@gomomento/sdk-core';
import { IAuthClient } from '@gomomento/sdk-core/dist/src/internal/clients';
import { AuthClientProps } from '../auth-client-props';
export declare class InternalAuthClient implements IAuthClient {
    private static readonly REQUEST_TIMEOUT_MS;
    private readonly creds;
    private readonly interceptors;
    constructor(props: AuthClientProps);
    generateAuthToken(scope: TokenScope, expiresIn: ExpiresIn): Promise<GenerateAuthToken.Response>;
    refreshAuthToken(refreshToken: string): Promise<RefreshAuthToken.Response>;
}
export declare function permissionsFromScope(scope: TokenScope): grpcAuth._GenerateApiTokenRequest.Permissions;
